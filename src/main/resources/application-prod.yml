#java -jar myApp.jar --spring.profiles.active=prod
server:
  port: 8088

spring:
  profiles: prod
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://192.168.0.50:3306/manufact?serverTimezone=UTC&characterEncoding=UTF-8&useSSL=false&useUnicode=true
#    username: test
#    password: test
  jpa:
    hibernate:
      ddl-auto: validate
      generate-ddl: true
      show-sql: true
      format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  servlet:
    multipart:
      maxFileSize: 50MB
      maxRequestSize: 100MB

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace

